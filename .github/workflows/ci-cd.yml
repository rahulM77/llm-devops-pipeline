name: LLMOps CI/CD Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    name: Lint & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          cd llm-devops-pipeline
          pip install fastapi uvicorn pydantic requests pytest pytest-asyncio ruff httpx

      - name: Lint with Ruff
        run: |
          cd llm-devops-pipeline
          ruff check app/ tests/ --ignore E501

      - name: Run unit tests
        run: |
          cd llm-devops-pipeline
          pytest tests/ -v --tb=short

  validate-prompts:
    name: Validate Prompt Versions
    runs-on: ubuntu-latest
    needs: lint-and-test
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Validate prompts
        run: |
          cd llm-devops-pipeline
          pip install fastapi uvicorn pydantic requests
          python pipeline/validate_prompts.py

  build-docker:
    name: Build Docker Image
    runs-on: ubuntu-latest
    needs: lint-and-test
    steps:
      - uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build image
        run: |
          cd llm-devops-pipeline
          docker build -f docker/Dockerfile -t llmops-api:test .
          echo "Docker build successful"

  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    needs: build-docker
    steps:
      - name: Simulate deployment
        run: |
          echo "Deploying to staging..."
          echo "All checks passed. Deployment successful."